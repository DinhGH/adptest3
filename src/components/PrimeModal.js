import React, { useState } from 'react'; function PrimeModal() { const [input, setInput] = useState(''); const [result, setResult] = useState([]); const isPrime = (num) => { if (num < 2) return false; for (let i = 2; i <= Math.sqrt(num); i++)       if (num % i === 0) return false; return true; }; const handleSubmit = () => { const numbers = input.split(' ').map(Number).filter(n => !isNaN(n)); const primes = numbers.filter(isPrime); setResult(primes); }; const styles = { container: { textAlign: 'center' }, input: { padding: '10px', width: '300px', fontSize: '16px', marginBottom: '10px' }, button: { padding: '10px 20px', fontSize: '16px', backgroundColor: '#007bff', color: '#fff', border: 'none', borderRadius: '5px', cursor: 'pointer' }, result: { marginTop: '15px', fontSize: '18px', color: '#333' }, }; return (<div style={styles.container}>       <h2>Prime Numbers</h2>       <input type="text" placeholder="Enter numbers separated by space" value={input} onChange={(e) => setInput(e.target.value)} style={styles.input} />       <br />       <button onClick={handleSubmit} style={styles.button}>Submit</button>       <div style={styles.result}>Result: {result.join(', ')}</div>     </div>); } export default PrimeModal; 